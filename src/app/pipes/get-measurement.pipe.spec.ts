import { GetMeasurementPipe } from './get-measurement.pipe';

describe('GetMeasurementPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMeasurementPipe();
    expect(pipe).toBeTruthy();
  });
});
